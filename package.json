{
  "name": "api-gateway",
  "version": "1.0.0",
  "description": "NginX API Gateway (Docker)",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean && docker-compose up --build",
    "clean": "docker rm api-gateway-nginx-1 && docker rm api-gateway-chess-1 && docker rmi api-gateway-nginx --force && docker rmi api-gateway-chess --force",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "api",
    "gateway",
    "nginx",
    "docker"
  ],
  "author": "",
  "license": "MIT"
}
